# --- Core ---
python-dotenv==1.0.1
pydantic==2.7.1
tqdm==4.66.4

# --- Ingestion ---
pypdf==4.2.0
beautifulsoup4==4.12.3
markdown-it-py==3.0.0
unstructured==0.14.4          # fallback loader, optionnel

# --- Chunking / NLP ---
nltk==3.8.1
tiktoken==0.7.0
langchain-text-splitters==0.2.0   # utilisé uniquement pour le RecursiveCharacterTextSplitter de fallback

# --- Embeddings ---
sentence-transformers==3.0.1      # bge-large-en / bge-m3 en local
openai==1.35.3                    # optionnel, si EMBEDDING_PROVIDER=openai

# --- Retrieval ---
rank-bm25==0.2.2
chromadb==0.5.3                   # base vectorielle locale, persistante sur disque

# --- Reranking ---
cohere==5.5.8                     # optionnel, si RERANKER_PROVIDER=cohere
# le cross-encoder local (sentence-transformers) est déjà couvert plus haut

# --- Génération ---
anthropic==0.28.0                 # LLM de génération de réponse (par défaut)

# --- Évaluation ---
ragas==0.1.9                      # framework d'évaluation "officiel", optionnel
datasets==2.20.0                  # dépendance de ragas

# --- Interface ---
streamlit==1.36.0
pandas==2.2.2
plotly==5.22.0

# --- Tests ---
pytest==8.2.2
